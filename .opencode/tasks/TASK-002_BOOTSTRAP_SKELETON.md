# TASK-002 — BOOTSTRAP APPROVED SKELETON

Use ONLY the architecture approved from TASK-001.

Create the minimum runnable project skeleton.

Requirements:

- root workspace/package manager config;
- frontend app;
- API app;
- worker app/process;
- shared package(s) only if approved;
- baseline env example;
- baseline formatting/lint/typecheck/test config;
- local orchestration config only if approved;
- minimal health endpoints/pages;
- no product upload/publishing implementation yet.

Acceptance:

- install succeeds;
- build/typecheck/lint baseline succeeds where configured;
- frontend/API/worker can start according to approved architecture.

Do not implement TASK-003+.
