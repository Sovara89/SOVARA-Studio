# TASK-003 — DATABASE + AUTH FOUNDATION

Implement approved database/ORM/migrations and real application user/session/account foundation.

Requirements:

- PostgreSQL connection/config;
- migrations;
- real user ownership model;
- auth/session flow suitable for Studio;
- external platform account model prepared for OAuth credentials;
- safe credential storage strategy;
- no hardcoded user ID;
- no plaintext secrets in logs/frontend.

Do not implement video upload yet except data structures explicitly approved here.
